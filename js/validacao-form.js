$(document).ready(function(){

	// Recupera os horários do localStorage e converte para um array
	//let horariosSelecionados = JSON.parse(localStorage.getItem("horariosSelecionados"));

	// Função para converter horário no formato "HH:MM" para minutos
	/*function converterParaMinutos(horario) {
	    let [horas, minutos] = horario.split(":").map(Number);
	    return (horas * 60) + minutos;
	}

	// Calcula a diferença total em minutos
	let minutosTotais = 0;
	for (let i = 1; i < horariosSelecionados.length; i++) {
	    let horarioInicio = converterParaMinutos(horariosSelecionados[i - 1]);
	    let horarioFim = converterParaMinutos(horariosSelecionados[i]);
	    minutosTotais += (horarioFim - horarioInicio);
	}

	// Exibe o resultado total
	console.log("Total de minutos:", minutosTotais);

	console.log('horariosSelecionados');*/

})